PORT=3000
JWT_SECRET=sleep_care_jwt_secret_2026

# 本地开发默认使用 SQLite，无需配置数据库连接
# DATABASE_TYPE=sqlite

# 腾讯云 CloudBase 生产环境使用 MySQL 时配置以下变量
# 同时兼容 PDF 示例中的 MYSQL_HOST / MYSQL_PORT / MYSQL_USER / MYSQL_PASSWORD / MYSQL_DATABASE
# DATABASE_TYPE=mysql
# DB_HOST=your-mysql-internal-host
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=your-password
# DB_NAME=sleep_care
